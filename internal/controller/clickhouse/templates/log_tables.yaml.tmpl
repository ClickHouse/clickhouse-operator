# System log tables grow without bound unless they carry a TTL, and they share
# the data volume with user data. Override via spec.settings.extraConfig.
{{- range .Tables }}
{{ . }}:
{{- if $.TTLDays }}
  ttl: event_date + INTERVAL {{ $.TTLDays }} DAY DELETE
{{- end }}
{{- end }}
